services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: sensum
      POSTGRES_PASSWORD: sensum
      POSTGRES_DB: sensum
    ports:
      - "5432:5432"
    volumes:
      - dbdata:/var/lib/postgresql/data

volumes:
  dbdata:
